//! Versioned batch envelope + payload formats.
//!
//! Wire-stable on-disk format that the cloud also accepts. Bump the
//! corresponding version constant when the format changes; minor-compatible
//! additions (new optional fields) do not require a version bump.

// Filled in by task #5.
